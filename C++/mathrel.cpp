#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    double x = 2;
    double y = 5;
    double z;
    //! MAX function
    // z = max(x,y);
    //! MIN function
    // z = min(x,y);
    //! POW function
    // z = pow(x,y);
    //! SQRT function
    // z = sqrt(x);
    //! ROUND function
    /*
    gives the nearest integer value
    z = round(2.3);
    */
    //! FLOOR function
    /*
    gives the previous integer value
    z = floor(2.6);
    */
    //! CEIL function
    /*
    gives the next integer value
    z = ceil(2.6);
    */
    //! ABS function
    /*
    gives positive value of the number
    z = abs(-2.6);
    */
    cout << z << endl;

    return 0;
}